# Public URL for the backend that issues LiveKit room tokens and proxies Higgs.
VITE_VOICE_GATEWAY_URL=http://localhost:8787

# Never put BOSON_API_KEY or a LiveKit API secret in a VITE_ variable.
# Keep both secrets in the gateway/runtime environment.
